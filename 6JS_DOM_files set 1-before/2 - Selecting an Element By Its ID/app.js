const heading = document.getElementById("heading"); 

const input = document.getElementById("input");

const button = document.getElementByI("button");

button.addEventListener("click", () => {
    heading.style.color = input.value;
   });  